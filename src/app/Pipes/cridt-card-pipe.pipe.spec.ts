import { CridtCardPipePipe } from './cridt-card-pipe.pipe';

describe('CridtCardPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CridtCardPipePipe();
    expect(true).toBe(true);
  });
});
